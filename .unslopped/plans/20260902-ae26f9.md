# Debt sweep, all: 4 item(s)

Cycle: 20260902-ae26f9
Started: 2026-09-02T19:17:57.904Z

## Goal
All four logged gaps are gone: the approve prompt always says what is pending or that nothing is, the proposals directory is gitignored so consuming a proposal never dirties the release tree, and the force-push and commit hook rules match real commands instead of prose and filenames.

## Approach
Chosen: the prompt reads the proposal file whenever it exists, cycle or not, and says plainly when nothing is pending. ensureGitignore learns the proposals path and the stale tracked file is untracked. The force-push rule gets the same command-position anchor the other rules got in the first sweep, and the commit rule requires commit as the git subcommand, tolerating dash options in between, so commit.md as a filename no longer matches.
Rejected: a shell parser, for the same reason as the first sweep; anchoring removes the observed misfires while keeping real invocations caught.

## Out of scope
- Any prompt changes beyond the approve family

## Files to touch
- src/init.ts
- src/hooks.ts
- test/sweep.test.ts
- test/init.test.ts
- CHANGELOG.md

## Relevant code
- .unslopped/skills/debt-sweep-all-7-item-s.md
- .unslopped/plans/20260902-0ba199.md
- src/loop.ts: DebtSelector:51, selectDebt():52, sweepGoal():58, seedPlanWithDebt():62, Candidate:8, nextCycleCandidates():18 (imported by 2)
- src/debt.ts: DebtCategory:6, DebtEntry:7, debtPath():12, addDebt():29, listDebt():39, removeDebtEntries():50 (imported by 5)
- .unslopped/DEBT.md

## Relevant skills
- debt-sweep-all-7-item-s (new, 1 run(s)): d:\Github\adderall\.unslopped\skills\debt-sweep-all-7-item-s.md
- show-the-evidence-when-an-approval-is-needed (new, 1 run(s)): d:\Github\adderall\.unslopped\skills\show-the-evidence-when-an-approval-is-needed.md
- make-pr-review-comments-plain-and-short (new, 1 run(s)): d:\Github\adderall\.unslopped\skills\make-pr-review-comments-plain-and-short.md

## Preferences
- PR review comments use plain language: short sentences, no jargon, severity said in human words  (global)
- Every command the tool asks a human to run must arrive with its context: what was verified, what it unlocks, how to decline  (global)
- Never stage .unslopped/DEBT.md in a feature commit or PR; it is an append-only log that conflicts on every parallel branch. Leave it in the working tree and commit it separately  (global)
- Commit messages follow Conventional Commits  (project, detected)
- Commit subjects stay under 50 characters  (project, detected)
- Package manager is npm  (project, detected)

## Debt items
- proposal files get committed and their consumption dirties the release tree; gitignore .unslopped/proposals (src/init.ts) :: logged 2026-09-02
- the force-push rule matches anywhere in command text; anchor it at command position (src/hooks.ts) :: logged 2026-09-02
- the commit rule matches the filename commit.md in git commands; require commit as the git subcommand (src/hooks.ts) :: logged 2026-09-02
- the approve prompt shows the proposal only when a cycle is active; show pending text whenever it exists and say when nothing is pending (src/hooks.ts) :: logged 2026-09-02

## Acceptance criteria
- [x] proposal files get committed and their consumption dirties the release tree; gitignore...
- [x] the force-push rule matches anywhere in command text; anchor it at command position (sr...
- [x] the commit rule matches the filename commit.md in git commands; require commit as the g...
- [x] the approve prompt shows the proposal only when a cycle is active; show pending text wh...

## Open questions

## Monitor
- First cycle run entirely through prompts: both human decisions arrived as harness prompts with their content attached, zero typed commands.
- The scope gate caught an unapplied plan edit (a silent string replace miss); worth preferring the editor tools for plan surgery.
- The gitignore fix proved itself within its own cycle: consuming the proposal left the release tree clean.
