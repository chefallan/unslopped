# Put the user journey in the README and help

Cycle: 20260902-6150d9

## Goal
People get the guide where they look for it: the README opens with the approved witty paragraph and carries a journey section naming who acts at each step, and `unslopped -h` works and opens with the loop and the five human moments before the command list.

## Approach
Chosen: adapt the published journey page into a compact README section between Quick start and the cycle table, focused on the three actors and the five human moments, leaving the long form to the phase table that follows. The help text gains a short journey header inside the existing HELP string, and `-h` joins `help` and `--help` in the dispatcher. One sentence there names where the long guide lives.
Rejected: printing the full journey in help, because help is a reference people grep, not a book. Rejected: a separate guide command, because a third place to look is worse than two good ones.

## Out of scope
- Protocol or instruction file changes (assistant behavior is unchanged)
- The published artifact page

## Files to touch
- src/cli.ts
- test/help.test.ts
- README.md

## Acceptance criteria
- [x] `unslopped -h` exits 0 and prints the same text as `unslopped help`
- [x] the help output states the loop and the five human moments before the command list
- [x] the README opening is the approved paragraph ending "It has to earn it."
- [x] the README carries a journey section that names who acts at each step and lists the five human moments
- [x] the full test suite passes

## Open questions

## Monitor
- First cycle whose release commit went through propose and approve; the flow held with zero friction beyond the intended pause.
- The journey now lives in three places on purpose: the artifact for sharing, the README for depth, help for the terminal glance. Same five moments in all three.
