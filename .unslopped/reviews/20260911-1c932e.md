- [minor] .github/workflows/unslopped-review.yml:21 the job resolves the latest published unslopped at run time, so a bad release reviews every pull request in this repository until it is yanked. That is the tradeoff for the workflow correcting itself after a publish, and it is worth knowing rather than discovering.
- [minor] .github/workflows/unslopped-review.yml:21 npx -y installs from the registry on every run with no lockfile and no integrity pin, so CI depends on npm being reachable and on whoever can publish this package name.
- [minor] the workflow will run against 0.1.3 until the publish, which does not carry the leanness scanner or the fixed commit guard. Every pull request reviewed before the publish gets weaker coverage than the README now describes.
