- [minor] src/practices.ts:433 the check proves a file exists and nothing more. An empty file, a stub, or a document last touched two years ago all pass. It catches a moved path, which is the failure it was built for, and it should not be mistaken for evidence that anyone reads the thing.
- [minor] src/hooks.ts:78 the docs listing prints on every session for the life of the project, so a repository listing six documents pays that cost in context on every prompt. Five entries is where this starts competing with the code map for attention.
- [minor] src/config.ts:116 an entry missing name or path is dropped silently on load, so a typo in the key removes a document from the listing with no warning anywhere. Failing loudly would be better, and matching how the rest of the config merge behaves is the reason it does not.
- [minor] the collision rule is prose with no check behind it, which the plan says plainly. It is the second prompt-level rule added this week after the report contract, and each one dilutes the claim that this tool advances on evidence rather than instruction.
