- [minor] src/practices.ts:419 the gate tells a human who has not answered yet that they got 0 of 2 right. A quiz recorded by `--file` starts with correct 0 and every question in missed, so quizCheck reports a score for an attempt nobody made. It should say the quiz is waiting for an answer while attempts is 0.
- [minor] src/cli.ts:398 the command deletes the path the caller passed to --file. A malformed file is rejected before the delete, so a source file cannot be lost this way, but the delete still acts on caller input and deserves a narrower target, such as requiring the file to sit outside the tracked tree.
- [minor] .unslopped/state.json holds the answer key in plain text, so a human who wants to defeat their own comprehension check can read it. The gate raises the cost of skipping the diff; it does not stop deliberate cheating. Recorded in the plan's handoff so nobody mistakes it for a security control.
- [minor] src/practices.ts:415 the threshold measures the whole cycle diff, not the pull request diff. On a cycle that spans several commits the number grows past what a reviewer of one PR sees. Correct for how unslopped scopes work, worth stating in the docs when the phase table row lands.
