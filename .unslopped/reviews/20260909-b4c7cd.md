- [minor] src/practices.ts:427 the check reads a marker, so an assistant can write "Rung 7" on work that rung 2 would have solved and pass. The rung is a claim for the human to check against the diff, not a proof. Worth stating in the failure text so nobody mistakes a passing check for a lean diff.
- [minor] src/practices.ts:260 the marker has to be the word rung plus a digit, so an approach that walks the ladder in prose without that exact phrasing fails. The failure prints the rungs, so the fix is quick, but the check is stricter than the rule it enforces.
- [minor] test/helpers.ts:56 twelve existing tests needed ladder off in the shared fixture. That is correct for a practice defaulting on, and it means any future default-on practice will need the same edit. The fixture is now the single place that decides what tests see, which is worth keeping deliberate.
- [minor] src/state.ts the plan template still offers a bare "## Approach" heading with no hint that a rung belongs there, so the first run of any cycle meets the rule as a gate failure rather than a prompt. Logged as debt.
