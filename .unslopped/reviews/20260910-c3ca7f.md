- [minor] src/lean.ts:14 the repository pass only reads javascript and typescript files while the diff pass reads any file that is not skipped, so the two passes disagree about coverage. A python file with a JSON deep clone is flagged in a diff and invisible to review --repo.
- [minor] src/lean.ts:12 the argument pattern allows one level of nested parentheses, so a filter callback with two nested calls is silently missed. That is the safe direction, a false negative rather than a false alarm, and it means the counts are a floor and not a total.
- [minor] src/lean.ts:60 the cap is spent in file order, so on a repository past the cap the later files are never read and the same twelve findings come back every run. Fixing the earliest file changes which findings appear, which reads as new problems appearing.
- [minor] src/lean.ts:53 the repository pass reads every tracked file one after another with no output until it finishes. On a large repository that is a long silence with no way to tell it is working.
